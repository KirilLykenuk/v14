#include <stdio.h>

int main() {
    int a = 15;
    int b = 0;
    int c = -3;
    int result;
    
    result = ((5 * a - b + c) * (5 * a - b + c)) / 2 + c + 1;
    
    printf("Значення функції: %d\n", result);
    
    return 0;
}
